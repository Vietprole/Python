cos ten de 3
ko ten de 4

de 0004

1. Viết chương trình sử dụng class để tính giá trị S với n nhập từ bàn phím
S = Tổng( (-1)^n * x^(2*n))/(2n)!) (n = 0 -> + vô cực)
2. Vẽ đồ thị các hàm số sau:
f(x) = (5 - x - x^2 -x^3) * exp(-(x^3 + x^2 +5x + 1)/(x-5)
G(x,y) = (5-x^3 -y^3 -x*y)*exp(-(x^3 - y^3 -xy -5)/(x-5))
3. Thực hiện huấn luyện tập cifar10 thỏa mãn các yêu cầu sau:
- Có các tập huấn luyện, kiểm chứng và kiểm thử (training, validation và testing) với tỉ lệ tùy chọn
- Sử dụng mạng CNN để huấn luyện tập dữ liệu trên (có vẽ đồ thị)
- Sau khi train xong thì lưu lại model
- Thực hiện load lại model vừa lưu và kiểm thử (kiểm thử ngẫu nhiên và lấy từ google hình)
- Lưu ý: Ưu tiên cải tiến hàm loss để tăng độ chính xác